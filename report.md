# 集成式生活工作台 · 完成说明

## 完成内容
搭建了一个**真实的在线生活工作台**，数据存放在服务端（非浏览器 localStorage），通过 REST API 读写，跨浏览器/刷新不丢。

## 交付物
- `G:\ai\2026-08-25-13-38-44\life-workspace\index.html` — 单页工作台前端
- `G:\ai\2026-08-25-13-38-44\life-workspace\server.js` — Node 后端（文件级持久化，原子写入）
- `G:\ai\2026-08-25-13-38-44\life-workspace\data\store.json` — 服务端数据文件（含示例数据）

## 架构与决策
- **存储方案**：「资料库」能力未以可调用工具/MCP 形式提供（find-skills 无匹配、官方文档无可用 API），为保证「数据真正在线、不只在浏览器本地」，改用**本地 Node 服务端 + JSON 文件持久化**，前端通过 fetch 直接读写 `/api/*`，所有增删改即时回写。
- **6 个模块均已落地**：记账理财（收支/预算/月度对比/分类饼图/筛选）、习惯健康（勾选·计数·数值三种打卡 + 30 天热力图）、减脂健身（体重体脂/7日均线/BMI/目标进度/热量缺口/自定义周计划）、日程统筹（逾期自动标红）、待 (清单)、书影音收藏（封面墙/列表双视图 + 年度统计）。顶部「今日概览」汇总关键指标并列出待办，逾期项标红。
- **可靠性**：先渲染界面再拉数据；读取/写入失败顶部弹出明确提示并可一键重试，避免白屏与静默丢数据；底部工具栏提供导出 JSON 备份 / 导入恢复。
- **视觉**：克制暖色高级风（陶土红主色、轻底深字、圆角、内联 SVG 图表），PC 左导航 / 移动端底部 Tab，适配手机。币种 ¥，支出红 / 收入绿。

## 可直接访问
已在本地启动开发服务器，内置浏览器预览面板打开 `http://localhost:8787` 即可使用。

## 注意 / 下一步
- **跨设备限制**：当前数据保存在本机沙箱服务端，**同一台机器换浏览器可用，但换设备需部署到公网**（如 CloudStudio/静态托管后端）。如需真正多端同步，我可帮你配置一键部署。
- 已预置少量示例数据（含 1 条逾期日程「提交季度报告」），可随时删除。
- 数据文件 `data/store.json` 切勿删除（属于项目数据，非临时缓存）。
