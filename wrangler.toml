name = "lifehub"
compatibility_date = "2024-09-23"
pages_build_output_dir = "public"

# Cloudflare KV 存储绑定：用于持久化所有模块数据（非浏览器本地）。
# 不在本文件写死 id，改为在 Cloudflare 控制台 Pages 项目
# 「设置 → 函数 → KV 命名空间绑定」中添加一个变量名为 LIFE_DB 的绑定，
# 否则占位符 ID 会导致部署阶段报 Invalid KV namespace ID。
# （Dashboard 绑定后无需修改本文件，保存即生效。）
